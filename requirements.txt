# Runtime
pyyaml>=6.0     # maaa_config — reads config/default.yaml
numpy>=1.24     # layers/l5_continuity.py — cosine recall over memory vectors
flask>=3.0      # api/maaa_api.py — REST API

# Development
pytest>=8.0
